#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# lint js and vue files
npx --no-install lint-staged

# CUSTOMIZATION: Docker environment compatibility fix
# Original code attempted to run `bundle exec rubocop` directly on host, which fails in Docker-only development
# Disabled rubocop pre-commit linting since bundle/rubocop only exist inside Docker containers
# Developers should manually run linting inside containers: docker compose exec base bundle exec rubocop
# Alternative: Re-enable if bundle is installed on host, or modify to use docker compose exec
# git diff --name-only --cached | xargs ls -1 2>/dev/null | grep '\.rb$' | xargs bundle exec rubocop --force-exclusion

# stage rubocop changes to files
# git diff --name-only --cached | xargs git add
